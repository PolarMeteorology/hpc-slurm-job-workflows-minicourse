# Sample data

`sample_sacct.csv` is a small teaching dataset that mimics selected fields from SLURM accounting output. It is used by `scripts/summarize_sacct.py` and the debugging/accounting episode.

`parameter_manifest.csv` describes array-task inputs for manifest-driven workflow examples.

`resource_observations.csv` contains completed-job observations used by `scripts/plan_resources.py`.
